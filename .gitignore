# dependencies
node_modules/

# build output
dist/
.astro/
.vercel/

# env (never commit the real password/secret)
.env
.env.local

# test artifacts
test-results/
playwright-report/
.playwright/

# OS / editor
Thumbs.db
.DS_Store
desktop.ini

# local agent / editor session files — never commit
.claude/

# pipeline intermediates (committed outputs are src/data + public/q only)
pipeline/out/
pipeline/out_spike/
# one-off audit/verification scratch (batch files + throwaway check scripts)
pipeline/_audit/
pipeline/_verify/

# large vendored dictionary — download per pipeline/README, never commit (~66MB)
pipeline/data/ecdict.csv
pipeline/__pycache__/
**/__pycache__/
