# dependencies
node_modules/

# build output
dist/
.astro/
.vercel/

# env (never commit the real password/secret)
.env
.env.local

# test artifacts
test-results/
playwright-report/
.playwright/
coverage/

# OS / editor
Thumbs.db
.DS_Store
desktop.ini

# local agent / editor session files — never commit（專案 skills 例外，要版控）
.claude/*
!.claude/skills/

# pipeline intermediates (committed outputs are src/data + public/q only)
pipeline/out/
pipeline/out_spike/
# one-off audit/verification scratch (batch files + throwaway check scripts)
pipeline/_audit/
pipeline/_verify/
pipeline/_guwen/

# large vendored dictionary — download per pipeline/README, never commit (~66MB)
pipeline/data/ecdict.csv
pipeline/__pycache__/
**/__pycache__/
**/.pytest_cache/
pipeline/_clean_report.txt
# explanation rollout scratch (transient batch output + one-off apply scripts)
pipeline/data/_batch.json
pipeline/data/_batch_apply.py

# 列印級 PDF 產出（可由 npm run pdf 重生）
exports/
